git_url(ENV["FASTLANE_MATCH_REPO"])
git_branch("main")

type("appstore")

app_identifier([ENV["BUNDLE_ID"]])
username(ENV["FASTLANE_USER"])
team_id(ENV["APPLE_TEAM_ID"])

# Use git (not keychains)
storage_mode("git")
